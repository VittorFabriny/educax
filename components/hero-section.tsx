import Image from 'next/image';
import { Button } from './button';
import { ArrowRight, Play } from 'lucide-react';


type heroProps = {
  variant: "landing" | "cursos" | "sobre"
}

export function HeroSection(props: heroProps) {
  return (
    <>
    <p></p>
    </>
  )
}